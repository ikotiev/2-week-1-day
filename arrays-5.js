let arr = ['Ильяс', 'Альви', 'Хамзат', 'Анзор', 'Мага', 'Адам','Ризван', 'Человек', 'Безымянный', 'спайдермэн'];
for (let i = 0; i < arr.length; i++) {
    if(arr[i][0] == 'А'){
        console.log(arr[i]);
    }
   
}