const numbers = ['JavaScript', 'Python', 'Java', 'C/С++', 'PHP', 'C#', 'Kotlin', 'Swift', 'Go', 'DataTransfer']
let i = 0
while (i < numbers.length) {
    if(numbers[i].length > 3){
        console.log(numbers[i]);
    }
  i++
}
